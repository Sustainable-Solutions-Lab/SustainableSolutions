# Google Sheets — published-CSV URLs (per tab)
# Sheet > File > Share > Publish to web > pick the tab > CSV > copy the URL
SHEET_PUBLICATIONS_CSV=
SHEET_PEOPLE_CSV=
SHEET_NEWS_CSV=
SHEET_FEATURED_CSV=

# Vercel deploy hook (used by the Apps Script edit trigger)
VERCEL_DEPLOY_HOOK_URL=
