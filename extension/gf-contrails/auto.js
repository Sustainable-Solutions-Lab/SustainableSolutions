// Runs before gf-bookmarklet.js (see manifest): marks this as an
// automatic context so the script polls for results, re-runs itself
// every few seconds (Google re-renders on sort/filter), and never alerts.
window.__sslContrailAuto = true;
